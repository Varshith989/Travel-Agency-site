import ContactClient from "@/components/contact/ContactClient";

export const metadata = {
  title: "Contact Concierge — Aerova Travels",
  description: "Connect with our luxury travel concierge. Phone, WhatsApp, email, or schedule a private lounge consultation.",
};

export default function ContactPage() {
  return <ContactClient />;
}
